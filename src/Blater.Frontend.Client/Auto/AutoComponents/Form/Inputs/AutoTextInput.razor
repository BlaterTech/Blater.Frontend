@inherits Blater.Frontend.Client.Auto.AutoBuilders.Base.BaseAutoFormComponent<string>
@implements Blater.Frontend.Client.Auto.AutoInterfaces.IAutoBuildableComponent

<div id="auto-text-input" class="@ExtraClass">
    <MudTextField
        id="@InputId"
        T="string"
        Disabled="Disabled"
        Immediate="true"
        Class="@ExtraClass"
        Placeholder="@PlaceholderText"
        Value="@Value"
        ValueChanged="@(async x => await NotifyValueChanged(x))"
        Label="@LabelName"
        For="@For"
        Error="@HasValidationError"
        ErrorText="@ValidationErrorMessage"
        RequiredError="@ValidationErrorMessage"
        Required="@Required"
        Variant="@Variant.Filled"
        />
</div>

@code
{
    public BaseAutoComponentTypeEnumeration ComponentType => AutoFormComponentInputType.AutoFormTextComponentInput;
}